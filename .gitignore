__pycache__/
*.egg-info/
.venv/
dist/
build/
*.html
!okflify/template.html
# the demo is built by CI and deployed to Pages — never committed, so it cannot
# drift from the renderer that produces it
_site/
.DS_Store
